export const STATIC_DESIGN_REVIEW_GUIDANCE = Object.freeze([
  "Use a full-screen or full-page export for product-level review.",
  "Avoid cropped screenshots unless the review scope is a single component or region.",
  "Include the intended viewport or device context in the filename or title when available.",
  "Treat findings as visual review notes, not live implementation audit results."
]);
